import { CreatePattern } from "@/components/ui/FloatingActionPresets";

// ... existing code ...

      <CreatePattern 
        onClick={() => setShowReportModal(true)}
        label="Report Issue"
      />
    </StandardPageLayout>
  );