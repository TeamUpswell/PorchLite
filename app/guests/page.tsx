import { CreatePattern } from "@/components/ui/FloatingActionPresets";

// ... existing code ...

      <CreatePattern 
        onClick={() => setShowAddGuestModal(true)}
        label="Add Guest"
      />
    </StandardPageLayout>
  );